export type {
    TickerUpdateMessage,
    DepthUpdateMessage,
    TradeUpdateMessage,
    WsMessage,
} from "@cex/shared";

export type { TradeUpdateMessage as TradeAddedMessage } from "@cex/shared";
